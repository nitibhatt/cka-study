# To do nslookup for a service, do the following:

kubectl run -it busybox --image=busybox --restart=Never -- nslookup svc-name.<namespace>.svc.cluster.local  

# To do nslookup for a pod, do the following:

kubectl run -it busybox --image=busybox --restart=Never -- nslookup podIp.<namespace>.pod.cluster.local, where podIp format is xx-xx-xx-xx
